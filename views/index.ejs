<%- include("partials/nav_bar.ejs",{sun_moon:"media/svg/icons8-sun.svg"}) %>
<!-- stuff go here  -->
<% var titles = title %> <% var num=0; %>
<div class="feeds">
    <% if (titles.length !=0){ %>
    <% titles.forEach(function(item){ %> 
        <% var idValue="feed-"+num; %>
         <% num++%>
        <%-include("partials/post.ejs",{title:item.title, idVal:item.id}) %>
         <% }) %>
         <%}else{%>
            <h3 class="empty"> It's a little empty try writing something...</h3>
            <% } %>
</div>
    
<%- include("partials/footer.ejs") %>
